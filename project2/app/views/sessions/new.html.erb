

<%= form_for @client, url: "/sessions", method: "post" do |f| %>

	<%= f.text_field :email, required: true, placeholder: "email" %>
	<%= f.password_field :password, required: true, placeholder: "password" %>
	<%= f.submit "Login" %>

<% end %>